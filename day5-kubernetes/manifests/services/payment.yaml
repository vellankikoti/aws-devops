apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: sock-shop
  labels:
    app: payment
spec:
  selector:
    app: payment
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
