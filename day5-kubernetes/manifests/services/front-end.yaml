apiVersion: v1
kind: Service
metadata:
  name: front-end
  namespace: sock-shop
  labels:
    app: front-end
spec:
  selector:
    app: front-end
  ports:
    - port: 80
      targetPort: 8079
      protocol: TCP
